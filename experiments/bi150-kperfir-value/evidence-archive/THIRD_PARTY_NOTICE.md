# Third-Party Evidence Notice

The Python files under `triton-profiler-package/` are unmodified snapshots from the installed CoreX 4.4.0 / Triton 3.1.0 distribution. They are retained as third-party environment evidence and are not KernelSwift-authored or KernelSwift-maintained source files.

The captured distribution metadata in [`triton-profiler-package/distribution-license-info.txt`](triton-profiler-package/distribution-license-info.txt) identifies:

- project URL: `https://github.com/triton-lang/triton/`;
- classifier: `License :: OSI Approved :: MIT License`.

The installed distribution metadata reported no separate license value, and the installed dist-info directory did not ship a separate license file. This notice records only the metadata and files observed in the captured distribution.
