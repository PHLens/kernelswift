# Skill Assets

本目录存放 `skills/` 文档使用的图示。每张图同时保留：

- `.svg`：可编辑源文件；
- `.png`：Markdown 默认展示文件。

当前图示：

- `kernel-opt-loop-flow.*`：语言无关的算子优化闭环；
- `unified-sketch.*`：Unified Sketch 的 D/O/C/H 结构；
- `track2-clike-architecture.*`：赛道二 C-like skill 适配架构；
- `track2-clike-roadmap.*`：赛道二基础设施和算子接入顺序。
